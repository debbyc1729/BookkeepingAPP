<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:background="#fefaec"
        android:icon="@drawable/wallet_icon"
        android:label="@string/app_name"
        android:logo="@drawable/wallet_icon"
        android:roundIcon="@drawable/wallet_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".chspend"
            android:label="@string/title_activity_chspend"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".TEST" />
        <activity android:name=".search" />
        <activity android:name=".addspend" />
        <activity android:name=".wallet" />
        <activity android:name=".creatwallet" />
        <activity android:name=".HOME">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HOME$HOME" />
    </application>

</manifest>